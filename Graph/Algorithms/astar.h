#include "../graph.h"
#include <vector>
#include <unordered_map>
using namespace std;

template<typename TV, typename TE>
vector<Vertex<TV, TE>*> astar(Graph<TV, TE>* graph, string start, string end, unordered_map<string, int> heuristics) {
    vector<Vertex<TV, TE>*> vertices;
    // TODO
    return vertices;
}