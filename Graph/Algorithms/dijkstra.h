#include "../graph.h"
#include <unordered_map>
using namespace std;

template<typename TV, typename TE>
unordered_map<Vertex<TV, TE>*, int> dijkstra(Graph<TV, TE>* g, string id) {
    unordered_map<Vertex<TV, TE>*, int> weights;
    // TODO
    return weights;
}